{
  "name": "@lootexchange/monorepo",
  "private": true,
  "description": "Loot Exchange monorepo",
  "license": "MIT",
  "scripts": {
    "deps:check": "npm-check-updates --deep --dep prod,dev,optional",
    "deps:update": "npm-check-updates -u --deep --dep prod,dev,optional"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=15",
    "yarn": ">=1.22.0",
    "npm": "please-use-yarn"
  },
  "devDependencies": {
    "npm-check-updates": "^11.8.5"
  }
}
